# macOS Finder metadata files.
.DS_Store

# SwiftPM build artifacts regenerated by `swift build`.
/.build

# Legacy SwiftPM package checkouts.
/Packages

# Xcode user-specific settings and build outputs.
xcuserdata/
DerivedData/

# SwiftPM IDE metadata that varies per developer.
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata

# Potential credentials for HTTP clients; keep secrets local.
.netrc
